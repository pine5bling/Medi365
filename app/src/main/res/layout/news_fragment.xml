<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/llNewsRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".presentation.news.NewsFragment">

    <com.thomnt.medi365.widget.HeaderView
        android:id="@+id/hvNews"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:hv_center_text="Tin tức"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvNews"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/hvNews"
        tools:listitem="@layout/news_item_layout" />

</androidx.constraintlayout.widget.ConstraintLayout>
